# Server
PORT=3000
NODE_ENV=production

# Session secret (REQUIRED in production - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=

# Admin credentials (only used on first run to seed the database)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
